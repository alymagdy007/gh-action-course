name: first work flow 

on: 
  push: 
    branches: 
      - main 

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcom message 
        run: echo "welcome to our githup action"

      - name: checkout repository 
        uses: actions/checkout@v6.1.0

      - name: list repository 
        run: ls -la


    
  
      
